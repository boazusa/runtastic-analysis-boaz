pipeline {
  agent any
environment {
    PATH = "C:/path/to/dir/containing/python.exe;$PATH"
}
  stages {
    stage('run_script') {
      steps {
        sh 'python read_runtastic_json.py'
      }
    }
    stage('version') {
      steps {
        sh 'python --version'
      }
    }
  }
}
