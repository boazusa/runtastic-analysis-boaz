pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        python3 read_runtastic_json.py
      }
    }
    stage('hello') {
      steps {
        sh 'python3 read_runtastic_json.py'
      }
    }
  }
}
