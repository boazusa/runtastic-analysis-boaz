pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        python --version
      }
    }
    stage('hello') {
      steps {
        python read_runtastic_json.py
      }
    }
  }
}
