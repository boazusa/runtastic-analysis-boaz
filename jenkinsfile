pipeline {
  agent any
  stages {
    stage('run_script') {
      steps {
        sh 'python read_runtastic_json.py'
      }
    }
    stage('version') {
      steps {
        sh 'python --version'
      }
    }
  }
}
